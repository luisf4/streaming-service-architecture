{
  "name": "@video-streaming/messaging",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run test/unit",
    "test:integration": "vitest run test/integration",
    "test:all": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@video-streaming/contracts": "workspace:*",
    "amqplib": "^0.10.5"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.5",
    "@video-streaming/config": "workspace:*",
    "testcontainers": "^10.16.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  }
}
